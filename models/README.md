# Model checkpoints

Training checkpoints will be written to this directory but excluded from Git because neural-network weights can be large.

The training stage will document the checkpoint format, the validation criterion used to select the best model, and the command needed to reproduce it.
